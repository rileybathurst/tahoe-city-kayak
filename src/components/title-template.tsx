// import React from 'react';
// TODO: this should come from a query
// import { useSiteName } from '../hooks/use-site-name';

// Im not 100 sure this is the best way but it works for now

// Hooks can only be called inside of the body of a function component.
// console.log(useSiteName());

// let x = useSiteName(); // just having this breaks the variable
// let y = 'hello';

const TitleTemplate = ' - Tahoe City Kayak & Padddleboard'; // original
// const TitleTemplate={` - ${useSiteName()}`};
// const TitleTemplate = 'test'; // yes
// const TitleTemplate={`test`}; // no
// const TitleTemplate = { y }; // no

export default TitleTemplate;